workspace:
  base: /go
  path: src/github.com/octocat/hello-world

pipeline:
  build:
    image: golang:latest
    commands:
    - go get ./...
    - GOOS="windows" GOARCH="386" go build -o targets/quality_server.exe
    - go build -o targets/quality_server
  publish:
    image: plugins/github-release
    api_key: repo
    files: targets/*